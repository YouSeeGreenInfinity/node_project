почта/пароль админа в сидах: admin@example.com / Admin123

План разработки

Этап 0: Подготовка и настройка проекта
+0.1. Инициализация проекта + создание репозитория
+0.2. Установка зависимостей
+0.3. Настройка TypeScript
+0.4. Структура проекта (создать папки)

Этап 1: Настройка базы данных и моделей
+1.1. Создать конфигурацию БД
+1.2. Создать модель User
+1.3. Создать вспомогательные модели/интерфейсы

Этап 2: Базовое приложение Express
+2.1. Создать основной файл приложения
+2.2. Создать точку входа
+2.3. Настроить .env файл

Этап 3: Сервисный слой и утилиты
+1. Создать сервисы
+2. Создать утилиты

Этап 4: Middleware и валидация
Middleware для аутентификации
2.Middleware для авторизации
3.Валидаторы

Этап 5: Контроллеры и маршруты
+1.Контроллеры
+2.Маршруты

Этап 6: Финальная сборка и тестирование

Структура API:

GET / → Информация о сервисе
GET /health → Проверка здоровья
GET /api → Информация о API
POST /api/auth/register → Регистрация
POST /api/auth/login → Авторизация
GET /api/auth/me → Текущий пользователь
GET /api/users → Список пользователей (admin)
GET /api/users/:id → Пользователь по ID
PUT /api/users/:id → Обновление пользователя
PATCH /api/users/:id/block → Блокировка/разблокировка
PATCH /api/users/:id/password → Смена пароля
DELETE /api/users/:id → Удаление пользователя (admin)

новые технологии:

swagger - (изначально — OpenAPI Specification) — спецификация для описания REST API. Это машиночитаемый и человекочитаемый файл, в котором подробно указано, какие есть конечные точки (эндпоинты), какие методы используются (GET, POST и т. д.), какие параметры и заголовки принимает запрос, и какой ответ вернёт сервер.

joi npm - "The most powerful schema description language and data validator for JavaScript."

concurrently - npm install concurrently --save-dev
